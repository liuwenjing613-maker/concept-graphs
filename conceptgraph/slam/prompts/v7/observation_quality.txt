只评判 CURRENT mask 的观测质量，不判断关联或新建。CONTEXT 用于定位，LOCAL RGB 看周边结构，TARGET ONLY 看 mask 内实际像素；不要把框内或背景中的完整物体当作目标。
同一实例指同一个独立的物理物体；属于同一套家具、相互接触或用途相关，不等于同一实例。沙发与放在其上的靠垫是两个实例；沙发自身的靠背、扶手则是同一实例的组成部分。仅当 mask 内实际像素同时包含沙发和靠垫时，按多实例混合判 INSUFFICIENT。
USABLE：主要对应一个可辨认物体或其有意义的局部；如果根据 mask 内实际像素及局部结构，非常确定多个不连续碎片来自同一个具体实例，也判 USABLE。小、遮挡、不完整或不连续本身不算无效；仅同类别、颜色相似或背景中存在完整物体，不足以确认这些碎片来自同一实例。
CORRUPTED：明确包含大量无关背景或明显错误碎片。
INSUFFICIENT：现有像素不足以判断，或明确混合多个独立实例；多实例混合优先判 INSUFFICIENT。
只返回 JSON，status 为上述三者之一，reason 用一句简短中文说明依据。