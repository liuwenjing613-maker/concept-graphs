# auto 全程无人工输入（最新用户要求）

auto 的所有未决新建/关联丢弃当前观测，未决合并保持不合并。不合并时仍检查完整点云双向包含率；超过 90% 仅记录冲突并保持不合并，按原两次拒绝规则计票，不暂停询问。human 模式保留包含率冲突的人工复核。

合并记录分别保存 exceeds_threshold（几何条件）与 requires_human（当前模式是否需要人工），避免把 auto 的冲突显示成待处理人工任务。运行入口记录 auto 不要求交互终端；人工入口增加 auto 保护，阻止任何意外调用读取 stdin。

32 项 V7 回归检查通过，包括超限 auto 不询问、输入失败并超限时 auto 不询问、human 保留询问、auto 入口防护、两次拒绝锁定等。零真实 API 调用，没有启动完整场景。日志：/home/chenkejun/beauty/v7_auto_no_human_20260907/smoke.log。

服务器代码与 GitHub 同分支更新。已启动的旧进程不会自动加载新代码，需要退出后重跑原 auto 命令。
