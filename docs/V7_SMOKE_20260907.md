# V7 VLMsplit smoke 阶段总结（2026-09-07）

实现分支 `ali-my-v7-VLMsplit`，服务器代码目录 `/home/chenkejun/beauty/conceptgraphs/code/experiments/ali-my-v7-VLMsplit`。
本阶段只验证接入和控制逻辑，未运行完整场景，未测建图准确率。

## 实现与配置

四类保留提示词及请求格式/采样参数已接入。合并身份明确采用用户确认的 RGB 投影＋放大图＋mask 背景说明版（原 26 例 22/26）。版本来源和哈希见代码 `conceptgraph/slam/prompts/v7/provenance.json`。
新建/关联的多 SAME 候选会生成全部两两合并事件；完成整组批准再合并并重新映射索引。同一对象对每帧最多记一票，连续两次合并才执行、累计两次不合并锁定；选中历史视角更新才解锁。仅不合并时检查双向完整点云包含率，任一方向严格 >90% 必须人工确认。距离按建图体素边长，本次 0.01 m。
`run_v7.sh human|auto` 均先运行同一组 VLM，仅未决事件处理方式不同。auto 丢弃未决观察/拒绝未决合并。两模式均保留包含率冲突的人工复核。默认模型 qwen3.6:35b-a3b-mtp-q4_K_M，11464 接口，建图 GPU 1。

## 检查及结果

- 29 项 v7 逻辑检查通过：包括票数、历史更新、A/B 反向不解锁、人工编号、接口失败、多 SAME 全配对、整组执行、索引更新、候选字母绑定。仅从 Git 暂存内容导出的干净目录再次检查通过，未依赖未提交的离线实验脚本。
- 原门控脚本 12 项、人工合并 14 项通过。bash/Python/页面 JavaScript 语法检查通过。结果页、复核页、数据 JSON 均 HTTP 200；复核数据正确保留等待题。未完成浏览器交互回归（桌面浏览器工具初始化失败）。
- 渲染仅复现一个 CURRENT 事件和用户保留 26 例中的 `m00002_f000004_periodic`。校验 active_manifest SHA-256 后取例，零 API；观测质量、候选身份、两张节点质量、合并身份图均与保留图片逐像素一致。需要隔离安装 Pillow 11.0.0/RAQM 与 OpenCV 4.12.0.88；原建图环境的不同字体/插值实现会产生像素差异。
- 真实 smoke 每次均从空图第 0 帧启动，只处理原始帧 0、5。临时 margin=100、关闭候选 IoU 预筛、max-events=2 用于覆盖观察链路；正式入口保留原触发默认值。
- `v7_auto_smoke_20260907_c`：节点质量真实调用 6 次，合并身份 2 次，分别累计 25.81 s 和 10.26 s。两个 SAME 均只记第一票，未误合并。第三个合并事件节点质量有 CONTAMINATED，auto 选择不合并；点云双向包含率 92.75% / 99.41%，正确暂停人工。开发者停止该短实验，没有填写或伪造人工答案。
- 最终 `v7_observation_smoke_20260907_e`：独立观察链路 smoke 临时 `--no-merge-review`，质量调用 2 次累计 7.94 s，候选身份 2 次累计 11.77 s，无接口/格式错误。一个质量不足观察 DISCARD，另一个经过 A/B 身份判断关联 A。完整短运行 exit=0 并保存地图。此短图仅用于流程检查，不能作为启用全部门控的正式建图结果。

## 失败、修复、局限

最初 a/b 两次 smoke 未调用 VLM：共享逐帧检测特征缓存被路径检查误拒绝。已只允许本次观察日志明确引用且哈希匹配的缓存特征，保留禁止未来/其他运行证据的检查。
c 的两次观察调用因 11465 已离线失败，auto 正确丢弃。默认接口已改为当前可用的同模型 11464，e 中真实调用正常。d 之后发现模板把候选字母固定为 B，已逐候选绑定并新增 A/B/C 请求检查，e 再次验证 A/B 真实请求。
未扩大 26 例评测，没有新增准确率结论。未验证完整场景下长时性能/建图质量；两票实际执行、多候选和 human 答案分支由合成小型检查验证，真实包含率冲突只验证到等待人工。

## 产物与下一步

原始日志、请求、耗时汇总和逐像素验证在 `/home/chenkejun/beauty/v7_split_smoke_20260907/validation.json` 及同目录日志；真实运行输出在 Replica/room0/exps/对应运行名。页面入口 `http://127.0.0.1:8895/v7VLM_v7_auto_smoke_20260907_c/`，人工页追加 `review/`。观察成功页运行名替换为 `v7_observation_smoke_20260907_e`。
下一步由用户运行完整场景，使用 README_V7.md 的 `.sh` 命令选择 human/auto；不复用任何 smoke 地图。
