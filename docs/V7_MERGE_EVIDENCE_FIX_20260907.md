# V7 多候选合并后关联记录修复

故障运行：v7_auto_20260907_230507_1779136，处理帧 8（原始帧 40）。
错误：`record_association_object_version: association target changed`。

原因：预先记录了本帧所有观察的旧关联目标与预期版本，然后才执行批准的多候选合并。主图正确把被合并节点的关联重定向到保留节点，但记录器仍预期旧 UID；即使 UID 不变，合并也会增加版本号，造成版本偏移。严格校验因此终止。此前小型检查只检查合并和索引，遗漏了真实证据记录回调。

修复：在合并前冻结相似度矩阵的列 UID 与版本；先执行批准的节点合并，再按当前节点及版本记录本帧观察关联，最后融合观察。相似度仍绑定原 H 快照，不伪造合并后分数，不关闭严格校验。

验证：新增 `tests/test_v7_merge_evidence.py`，使用真实 Open3D 合并、真实观察融合与 strict EvidenceRecorder，覆盖被删除目标的重定向、同帧两条观察进入同一保留节点、其他节点索引移动、NEW/DISCARD，以及目标版本连续递增。通过。29 项 v7 回归和 6 项原证据记录测试通过，Python 编译通过。全部为小型组件 smoke，零 VLM 请求，没有重跑完整场景。该组件样本没有完整 RGB/depth 归档，因此以 component_smoke 状态关闭，不冒充完整场景审计。

服务器日志：`/home/chenkejun/beauty/v7_merge_evidence_fix_20260907/`。
用户自行重跑原 `bash run_v7.sh auto --gpu 1` 命令即可，入口自动创建新目录并从第 0 帧开始；保留故障运行原始证据，不续用故障时的部分地图。
