# v7_CLIP_must_v2 H800 切换记录

用户于2026-09-14要求切换H800。建图继续使用5880 GPU5，推理使用H800上的Qwen3.6-35B-A3B-FP8/vLLM。原Q4部分运行主动停止，保留原始日志，不用于正式完整指标。

正式版本：cf15543142792baebcae31851fefd1255ab29e77。正式输出为 Replica/room2/exps/v7_CLIP_must_v2_room2_H800_20260914；日志 room2_h800.log，PID记录room2_h800.pid。从0帧空图启动，end=2000，stride=5，共400帧，独立空质量缓存，前端和RGB/depth校验沿用已验证输入。

本次只增加H800传输与真实模型指纹绑定。质量提示词、渲染图、唯一anchor关联、独立节点合并、动态一票/两票、失败回退与缓存规则不变。模型文件逐文件SHA256保存在h800/model_identity.json。接口参数转发保持图片字节、PNG/JPEG类型、JSON schema、temperature=0、num_predict=1024及禁用thinking；32768上下文与服务一致。两端口共享H800模型，保留最多两路并发。

检查结果：原版本78项测试已通过；切换后67项v7相关测试通过（新增3项，不与此前测试数简单相加）。8例同图质量验证无接口/解析失败；新快照绑定及缓存命中通过。4帧在线检查PASS，严格审计PASS；38次HTTP，15次质量阶段，1次缓存命中，1次负面复核；两次一票MERGED，两次两票DEFERRED，无证据日志错误。规则审计另有26条语义疑点，它们不是视觉GT错误统计。

同8个输入的初判HTTP平均：5880 Q4 12.1128秒，H800 FP8 1.3176秒，约9.19倍。包含不同数量复核的全部HTTP平均分别11.9543秒/10次及1.1887秒/13次；不能将其直接作为配对初判或整场加速比。8例最终标签有3例不同；样本仅用于接口/分支验证，不能将Q4质量优化准确率直接归给FP8，也不据此宣称FP8更准。原始请求、响应及timing保留于h800_live_quality_smoke及h800/adapter_requests。

模型切换已获用户授权；原must也是FP8，但最终对比仍需核实推理配置和输入协议。正式运行完成后计算完整指标并与must、v7_CLIP、OVIMAP比较，保留成功、退化、失败与局限。结果只更新原有统一指标.xlsx。每10分钟监控，全部评测、对比、同步校验完成后按已有授权关闭本任务H800/5090；共享5880绝不关机。

结果页：http://127.0.0.1:18927/v7VLM_v7_CLIP_must_v2_room2_H800_20260914/
复核页：http://127.0.0.1:18927/v7VLM_v7_CLIP_must_v2_room2_H800_20260914/review/
